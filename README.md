Invite codes expire after 2 days. Logout button was added to all user type homepages. Additionally, added a back button to some of the admin pages to allow traveling back to the homepage instead of having to close the entire application. 

The database was updated to support a user having multiple roles, as well as the admin page that lists all users. Additionally, the login functionality was updated: users with only one role are automatically taken to that roles homepage, whereas users with multiple roles are taken to a screen where they select the role they would like to login as.

The one-time-password functions and their generation, validation, and deletion, and the display of a table of users and their data.

Added in the code for automatically going to the homepage for single role users instead of the welcome page while users with multiple roles will go to the welcome login page. Also added in the code that will allow admins to delete users that are no longer in the class from the database. 

The Admin and User set up stories were implemented. All the updates provide the functionality showing the input box suggestion and context information of username and password on admin and user page and the username and password validation and unit tests for those functions. All those inputbox can update the database.

The Java files for the program are located in FoundationCode/src
The Standup Meetings folder contains 5 video files, one for each standup meeting as well as Christians recording for standup 2
The Standup Meetings folder also contains a text document containing YouTube links to both screencasts
